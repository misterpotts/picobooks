You are working in the tiny-ledger assessment repository.

Plan the next implementation change before editing code.

Use these constraints:
- Java 25.
- Spring Boot 3.5.x.
- Spring MVC / servlet, not WebFlux.
- Virtual threads enabled.
- In-memory storage only.
- Keep the solution small.
- Plan work for a non-main branch and a human-reviewed PR.
- Commit messages and PR titles must use Conventional Commits.
- Preserve OpenSpec workflow: active deltas stay under ignored `openspec/changes/**`; applied behaviour goes to `openspec/specs/**`.

Produce:
1. a short implementation plan;
2. the OpenSpec delta that should be copied into the PR description;
3. the files expected to change;
4. the tests expected to prove the change;
5. the expected Conventional Commit subject and PR title;
6. explicit non-goals.

Do not edit files during this planning pass.
